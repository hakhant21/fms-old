http:  
  services:
    backend-service:
      loadBalancer:
        servers:
          - url: "https://backend:9000"
          - url: "https://backend1:9000"
          - url: "https://backend2:9000"
        strategy: "LeastConn"
  
  serversTransports:
    insecure-skip-verify:
      insecureSkipVerify: true